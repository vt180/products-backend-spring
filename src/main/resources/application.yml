spring:
  application:
    name: "products-backend-spring"
  kafka:
    bootstrap-servers: localhost:29092

kafka:
  topic:
    name: products
  groupId: product-group-1

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
